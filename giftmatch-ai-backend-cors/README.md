# Giftmatch.ai Gemini Backend

This version includes CORS, so the GitHub Pages website can call the Vercel API.

Required Vercel Environment Variable:

GEMINI_API_KEY=your Gemini API key

API route:

/api/gift
